import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    form: {
      gap: 15,
    },
    input: {
      borderWidth: 1,
      borderColor: '#ccc',
      borderRadius: 8,
      padding: 12,
    },
    error: {
      color: 'red',
      marginBottom: 5,
      fontSize: 12,
    },
  });

  export default styles;